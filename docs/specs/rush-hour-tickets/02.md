## Parent

#3

## What to build

Keep cart rows stable and flush edits before checkout so cashiers can continue serving during a busy shift without losing correctness.

## Acceptance criteria

- [ ] Updating one line does not rerender unchanged cart rows due only to recreated modifier arrays.
- [ ] Pending debounced quantities are awaited before checkout; failed edits block transition and remain retryable.
- [ ] Rapid input, changing rows, unmount and failures are covered through cart interaction tests.

## Blocked by

None — can start immediately.

