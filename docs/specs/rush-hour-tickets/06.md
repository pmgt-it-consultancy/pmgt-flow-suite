## Parent

#3

## What to build

Measure sync phases and preserve queued work so cashiers can continue serving during a busy shift without losing correctness.

## Acceptance criteria

- [ ] Opt-in bounded numeric diagnostics measure pull/map/apply/push phases without storing customer payloads.
- [ ] A push requested during in-flight sync is not dropped; retry/offline/stop behavior remains correct.
- [ ] Sync yields between pages; tests cover queued work and diagnostics retention.
- [ ] Document evidence requirements before introducing workers or an index migration.

## Blocked by

None — can start immediately.

