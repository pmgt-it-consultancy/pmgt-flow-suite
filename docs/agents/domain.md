# Domain docs

Single-context layout: root CONTEXT.md for domain vocabulary; docs/adr/ for architectural decisions.

Read the glossary and relevant ADRs before exploring a domain. If absent, proceed; create lazily only when terms or decisions are resolved. Use existing domain names and explicitly surface conflicting ADRs. Implementation details belong in specs, not the glossary.

