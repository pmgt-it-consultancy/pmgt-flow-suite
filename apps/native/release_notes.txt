## What's New

- Fixed unresponsive buttons in several modals, including the in-app update prompt, the tab selection sheet, the guest count (PAX) entry modal, the auto-lock timeout picker, and the system status dropdown
