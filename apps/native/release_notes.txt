## What's New

- Improved Refresh POS Data so large operational snapshots are downloaded in safe, bounded pages.
- Verified offline takeout ordering and reconnect delivery against a 10,000-order staging restaurant.
- Added an isolated staging simulation for ongoing tablet-scale sync validation.

## Notes

- The bounded sync engine remains behind its rollout flag. Existing POS synchronization stays active by default.
- Cold launches without connectivity still require the authentication cache follow-up before full offline-first rollout.
