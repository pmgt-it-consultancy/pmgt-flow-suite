diff --git a/native/android-jsi/src/main/cpp/CMakeLists.txt b/native/android-jsi/src/main/cpp/CMakeLists.txt
index 79d961e8a1b3a292d93cd5bfc4dff303d7151cd6..ee8628a5f0383609f9d49d2ca6f81024b8d32bf7 100755
--- a/native/android-jsi/src/main/cpp/CMakeLists.txt
+++ b/native/android-jsi/src/main/cpp/CMakeLists.txt
@@ -14,6 +14,10 @@ set(WMELON_JSI_BUMP 4)
 
 get_filename_component(_nativeTesterPath "../../../../../node_modules/@nozbe/sqlite/" REALPATH)
 get_filename_component(_nozbePath "../../../../../../../../../native/node_modules/react-native/ReactCommon/jsi/jsi/" REALPATH)
+# Detects pnpm layout: when WatermelonDB is installed under
+# node_modules/.pnpm/<pkg>/node_modules/@nozbe/watermelondb/, three extra
+# directory levels separate the CMakeLists from the top-level node_modules.
+get_filename_component(_pnpmPath "../../../../../../../../../../react-native/ReactCommon/jsi/jsi/" REALPATH)
 
 if(EXISTS "${_nativeTesterPath}")
         # these paths work for WatermelonDB native tester
@@ -23,6 +27,14 @@ elseif(EXISTS "${_nozbePath}")
         # these paths work for Nozbe
         set(NODE_MODULES_PATH_WM ../../../../../../../)
         set(NODE_MODULES_PATH_RN ../../../../../../../../../native/node_modules/)
+elseif(EXISTS "${_pnpmPath}")
+        # these paths work for pnpm:
+        #   - WMDB's @nozbe/* subdeps live inside .pnpm/<wmdb>/node_modules/
+        #     (7 ups, same as the standard branch)
+        #   - react-native is hoisted to the top-level node_modules
+        #     symlink (10 ups: 7 to land in @nozbe, +3 for .pnpm/<pkg>/node_modules)
+        set(NODE_MODULES_PATH_WM ../../../../../../../)
+        set(NODE_MODULES_PATH_RN ../../../../../../../../../../)
 else()
         # these paths should work for a standard RN project
         set(NODE_MODULES_PATH_WM ../../../../../../../)
